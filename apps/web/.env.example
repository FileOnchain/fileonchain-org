# Public origin used for canonical URLs, Open Graph, sitemap.xml, and robots.txt.
# Origin only, no trailing slash. Falls back to https://fileonchain.org when unset.
NEXT_PUBLIC_SITE_URL=https://fileonchain.org

# Google Analytics 4 measurement id (looks like G-XXXXXXXXXX). Leave empty to
# disable analytics — the <GoogleAnalytics> tag only mounts when this is set.
NEXT_PUBLIC_GA_ID=

# Google Search Console verification token (the `content` value from the
# "HTML tag" verification method). Server-only — no NEXT_PUBLIC_ prefix. When
# set, a <meta name="google-site-verification"> tag is emitted in <head>.
GOOGLE_SITE_VERIFICATION=

# Neon Postgres pooled connection string (postgres://…-pooler.…neon.tech/…).
# Required for auth, the dashboard, credits, API keys, and BYOK.
DATABASE_URL=

# Auth.js session encryption secret. Generate with: openssl rand -base64 32
AUTH_SECRET=

# Canonical app URL for Auth.js callbacks. Optional — inferred in most
# environments; set explicitly for local dev if callbacks misbehave.
# AUTH_URL=http://localhost:3000

# OAuth sign-in providers. All optional — a provider's button is hidden on
# /login when its credentials are unset, so wallet sign-in works without them.
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# 32-byte base64 key used to AES-256-GCM encrypt BYOK provider API keys at
# rest. Generate with: openssl rand -base64 32
# Also seals per-org Cloud signer keys (server_sign).
BYOK_ENCRYPTION_KEY=

# Shared secret guarding scheduled cron routes (retention sweep). Vercel Cron
# sends it as `Authorization: Bearer $CRON_SECRET`. Generate with:
# openssl rand -base64 32. Absent = /api/cron/* rejects all requests.
CRON_SECRET=


# Anchor attribution. The platform id the server worker carries in anchor
# payloads (attribution only). FileOnChain itself is platform 1 (the default).
ANCHOR_PLATFORM_ID=
# The browser pay-as-you-go flow attributes anchors to this platform id
# (falls back to platform 1).
NEXT_PUBLIC_FILEONCHAIN_PLATFORM_ID=

# Funded server signers for the credits/BYOK anchor worker. All optional —
# when unset (or the chain has nothing deployed), the worker falls back to a
# deterministic mock anchor. Fund these accounts on every chain they serve.
# EVM: 0x-prefixed private key used for FileRegistry anchorChunk event sends.
ANCHOR_EVM_PRIVATE_KEY=
# Substrate: secret URI (mnemonic or //derivation) for remark anchors.
ANCHOR_SUBSTRATE_SEED=
# Solana: keypair for SPL Memo anchors — base58 string or the JSON byte
# array a solana-keygen file contains.
ANCHOR_SOLANA_SECRET_KEY=
# Aptos: hex ed25519 private key for file_registry::anchor_cid module calls.
# Real sends also need `moduleAddress` set on the aptos:* chain entries.
ANCHOR_APTOS_PRIVATE_KEY=
# Cosmos: BIP-39 mnemonic; anchors ride memos on 1-unit self-sends.
ANCHOR_COSMOS_MNEMONIC=
# Sui: `suiprivkey…` export of an ed25519 keypair; needs `moduleAddress`.
ANCHOR_SUI_PRIVATE_KEY=
# Starknet: accounts are contracts, so the deployed account address is
# needed alongside its signing key; needs `registryContract`.
ANCHOR_STARKNET_ACCOUNT=
ANCHOR_STARKNET_PRIVATE_KEY=
# NEAR: account id plus an "ed25519:…" full-access key; needs `moduleAddress`
# (the registry contract account).
ANCHOR_NEAR_ACCOUNT_ID=
ANCHOR_NEAR_PRIVATE_KEY=
# TRON: hex secp256k1 private key; anchors ride `extra_data` on 1-SUN
# self-sends via the TronGrid REST API.
ANCHOR_TRON_PRIVATE_KEY=
# Cardano: CLI payment signing key (cborHex) + a Blockfrost project id
# matching the network — anchors ride tx metadata (CIP-20 label 674).
ANCHOR_CARDANO_SIGNING_KEY=
ANCHOR_CARDANO_BLOCKFROST_KEY=
# TON: wallet v4 mnemonic; anchors ride transfer comments. The toncenter
# API key is optional but avoids public rate limits.
ANCHOR_TON_MNEMONIC=
ANCHOR_TON_API_KEY=
# Hedera: operator account (0.0.x) + private key submitting HCS messages;
# needs `hcsTopicId` on the hedera:* chain entries.
ANCHOR_HEDERA_OPERATOR_ID=
ANCHOR_HEDERA_PRIVATE_KEY=

# Upload Advisor — optional OpenRouter key for LLM headline/rationale copy
# only. The deterministic rule engine runs without it; suggested settings
# never come from the model. Server-only.
OPENROUTER_API_KEY=
# OpenRouter model slug for the copy layer (default openai/gpt-4o-mini).
RECOMMENDATION_LLM_MODEL=
# Set to "0" to force template copy even when an OpenRouter key is present.
RECOMMENDATION_LLM_ENABLED=
# Feature flag for the Upload Advisor UI. Set to "0" to hide the component
# entirely (no advisor API calls); any other value (or unset) enables it.
# Users can also turn it off per-account in Preferences.
NEXT_PUBLIC_UPLOAD_ADVISOR_ENABLED=

# Cloud evidence surface — gates /api/v1/{evidence,agent-runs,verify,retention}
# + the /cloud/* webapp pages. The backend, schema, and pages ship in this
# build; the routes are not reachable until you set this to "1". Per the
# ops runbook at docs/deploy/cloud.md, this is the first flag to flip when
# opening the commercial surface.
# FILEONCHAIN_CLOUD_EVIDENCE_ENABLED=1

# Project-tenancy flag — gates /cloud/projects, project-scoped API keys,
# per-project Cloud signers, and the quota gates on the evidence /
# agent-run / anchor routes. Requires the evidence flag to also be on.
# Flip this once projects are ready to ship (after the evidence flag is on).
# FILEONCHAIN_CLOUD_TENANCY_ENABLED=1

# Webhooks flag — gates /api/v1/webhooks, /cloud/webhooks, and the per-
# minute webhooks-drain cron. The flag is independent of the evidence flag.
# Flip this when webhook receivers are in place.
# FILEONCHAIN_CLOUD_WEBHOOKS_ENABLED=1

# Bulk-export flag — gates /api/v1/exports, /cloud/exports, and the daily
# exports-sweep cron. The flag is independent of the evidence flag.
# Flip this when off-site ledger retention is in place.
# FILEONCHAIN_CLOUD_EXPORTS_ENABLED=1

# Compliance-reports flag — gates /api/v1/compliance-reports, /api/v1/sla,
# /cloud/compliance, and the monthly compliance-reports-build cron. The
# flag is independent of the evidence flag. Flip this when the SLA tier
# model is ready to ship to paying tenants.
# FILEONCHAIN_CLOUD_COMPLIANCE_ENABLED=1

# Rate-limit overrides for the `/api/v1/*` API-key surface. Values are
# per-minute requests per scope. Absent = the hard-coded defaults in
# `lib/server/rate-limit.ts` apply (600/min api-key, 60/min ip, 60/min
# POST /v1/anchor, 120/min POST /v1/evidence). Tune these without a
# deploy by changing the env and redeploying.
# RATE_LIMIT_V1_PER_MIN=
# RATE_LIMIT_V1_ANCHOR_PER_MIN=
# RATE_LIMIT_V1_EVIDENCE_PER_MIN=
# RATE_LIMIT_V1_IP_PER_MIN=

# Auto Drive account-probe endpoint base URL used by the BYOK validator
# in `lib/server/byok.ts`. Absent = the public mainnet endpoint at
# https://mainnet.auto-drive.autonomys.xyz/api. Set this to a staging
# mirror when validating user keys in test environments.
# AUTODRIVE_API_URL=

# BIP-39 mnemonic for the treasury that owns the per-user deposit-address
# tree. Each user gets a stable EVM address derived as
# m/44'/60'/0'/0/<hashed(userId)>'/0/0; the treasury sweeps incoming
# USDC into a hot wallet. Absent = POST /api/credits/deposit returns 503.
# DO NOT USE A DEV MNEMONIC IN PRODUCTION — generate a fresh 24-word seed.
# DEPOSIT_TREASURY_MNEMONIC=
