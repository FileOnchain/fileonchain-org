/**
 * `@fileonchain/sdk/protocol` — the FileOnChain Evidence Protocol core
 * (re-export of `@fileonchain/protocol`): neutral envelope types,
 * canonical encoding, digests, signing payloads, adapters, validation,
 * and legacy migration. Kept behind a subpath so its names never collide
 * with the root entry's legacy vocabulary.
 */
export * from "@fileonchain/protocol";
