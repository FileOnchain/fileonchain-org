/**
 * `@fileonchain/sdk/agent-profile` — the FileOnChain Agent Evidence
 * Profile (re-export of `@fileonchain/agent-profile`): opinionated claims
 * and validation for AI-agent runs, tool calls, approvals, and policies.
 */
export * from "@fileonchain/agent-profile";
