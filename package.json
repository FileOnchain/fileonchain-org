{
  "name": "fileonchain",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @fileonchain/web dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @fileonchain/web start",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
